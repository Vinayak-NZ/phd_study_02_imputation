## ---- selfcare-J2R

#MCAR-20

sc_locf_mcar_20 <- simulate_locf_bulk(input = sc_missing_mcar_20, 
                                    var = "selfcare")

#MCAR-30

sc_locf_mcar_30 <- simulate_locf_bulk(input = sc_missing_mcar_30, 
                                    var = "selfcare")

#MCAR-40

sc_locf_mcar_40 <- simulate_locf_bulk(input = sc_missing_mcar_40, 
                                    var = "selfcare")

#MAR-20

sc_locf_mar_20 <- simulate_locf_bulk(input = sc_missing_mar_20, 
                                   var = "selfcare")

#MAR-30

sc_locf_mar_30 <- simulate_locf_bulk(input = sc_missing_mar_30, 
                                   var = "selfcare")

#MAR-40

sc_locf_mar_40 <- simulate_locf_bulk(input = sc_missing_mar_40, 
                                   var = "selfcare")

#MNAR-20

sc_locf_mnar_20 <- simulate_locf_bulk(input = sc_missing_mnar_20, 
                                    var = "selfcare")

#MNAR-30

sc_locf_mnar_30 <- simulate_locf_bulk(input = sc_missing_mnar_30, 
                                    var = "selfcare")

#MNAR-40

sc_locf_mnar_40 <- simulate_locf_bulk(input = sc_missing_mnar_40, 
                                    var = "selfcare")

## ---- physical-activity-J2R

#MCAR-20

pa_locf_mcar_20 <- simulate_locf_bulk(input = pa_missing_mcar_20, 
                                    var = "physical_activity")

#MCAR-30

pa_locf_mcar_30 <- simulate_locf_bulk(input = pa_missing_mcar_30, 
                                    var = "physical_activity")

#MCAR-40

pa_locf_mcar_40 <- simulate_locf_bulk(input = pa_missing_mcar_40, 
                                    var = "physical_activity")

#MAR-20

pa_locf_mar_20 <- simulate_locf_bulk(input = pa_missing_mar_20, 
                                   var = "physical_activity")

#MAR-30

pa_locf_mar_30 <- simulate_locf_bulk(input = pa_missing_mar_30, 
                                   var = "physical_activity")

#MAR-40

pa_locf_mar_40 <- simulate_locf_bulk(input = pa_missing_mar_40, 
                                   var = "physical_activity")

#MNAR-20

pa_locf_mnar_20 <- simulate_locf_bulk(input = pa_missing_mnar_20, 
                                    var = "physical_activity")

#MNAR-30

pa_locf_mnar_30 <- simulate_locf_bulk(input = pa_missing_mnar_30, 
                                    var = "physical_activity")

#MNAR-40

pa_locf_mnar_40 <- simulate_locf_bulk(input = pa_missing_mnar_40, 
                                    var = "physical_activity")